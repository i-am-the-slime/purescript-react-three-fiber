module React.Three.Drei where

import React.Basic (ReactComponent)

foreign import roundedBox ∷ ∀ a. ReactComponent a

foreign import html ∷ ∀ a. ReactComponent a

foreign import perspectiveCamera ∷ ∀ a. ReactComponent a

foreign import presentationControls ∷ ∀ a. ReactComponent a

foreign import orbitControls ∷ ∀ a. ReactComponent a

foreign import mapControls ∷ ∀ a. ReactComponent a